interface ConfigProps {
    appName: string;
    appDescription: string;
    domainName: string;
}

const config = {
    appName: "SUBBD",
    appDescription:
        "The ultimate AI powered content creation platform. Empowering creators with AI innovation in a web3 ecosystem.",
    domainName: "subbd.com",
} as ConfigProps;

export default config;
